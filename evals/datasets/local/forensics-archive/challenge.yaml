id: forensics-archive
title: Archive Carve
category: forensics
description: zip archive carving and hexdump toy.
files:
  - blob.bin
flag_regex: flag\{[A-Za-z0-9_]+\}
metadata:
  expected_flag: flag{forensics_zip}
  max_time: 60
  difficulty: easy
  tags:
    - forensics
    - triage
  required_tools:
    - python3
    - file
    - strings
