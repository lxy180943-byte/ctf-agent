id: web-params
title: Web Params
category: web
description: parameter fuzz form identification toy.
files:
  - app.html
flag_regex: flag\{[A-Za-z0-9_]+\}
metadata:
  expected_flag: flag{web_params}
  max_time: 60
  difficulty: easy
  tags:
    - web
    - triage
  required_tools:
    - python3
    - curl
