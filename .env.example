# Copy to .env locally if your shell workflow loads dotenv files.
# Values below are placeholders only; never commit a real API key.
OPENAI_API_KEY=replace-with-your-openai-api-key
OPENAI_BASE_URL=replace-with-openai-compatible-base-url
OPENAI_MODEL=replace-with-model-name
